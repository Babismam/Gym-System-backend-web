<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                                 https://jakarta.ee/xml/ns/persistence/persistence_3_2.xsd"
             version="3.1">

    <!-- ΑΛΛΑΓΗ 1: transaction-type="RESOURCE_LOCAL" (Απαραίτητο για Docker/Railway) -->
    <persistence-unit name="GymPU" transaction-type="RESOURCE_LOCAL">

        <!-- ΑΛΛΑΓΗ 2: Ο Provider πρέπει να μπει ΠΡΙΝ από τα classes -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>com.example.gymmanagement.entities.User</class>
        <class>com.example.gymmanagement.entities.Program</class>
        <class>com.example.gymmanagement.entities.Attendance</class>
        <class>com.example.gymmanagement.entities.MemberProgram</class>
        <class>com.example.gymmanagement.entities.RolePermission</class>
        <class>com.example.gymmanagement.entities.Schedule</class>
        <class>com.example.gymmanagement.entities.TrainerSchedule</class>

        <properties>
            <!-- Ρυθμίσεις Hibernate -->
            <property name="jakarta.persistence.schema-generation.database.action" value="update"/>
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>

            <!-- ΑΛΛΑΓΗ 3: JDBC Properties (Διαβάζουν τις μεταβλητές του Railway) -->
            <property name="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver"/>

            <!-- Στο Railway Variables βάλε: DB_URL, DB_USER, DB_PASSWORD -->
            <property name="jakarta.persistence.jdbc.url" value="${DB_URL}"/>
            <property name="jakarta.persistence.jdbc.user" value="${DB_USER}"/>
            <property name="jakarta.persistence.jdbc.password" value="${DB_PASSWORD}"/>

            <!-- Logs -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>